<div class="container">
  <h1>チーム編集の予定</h1>
  <%= form_with model: @team, method: :patch do |f| %>
  <%= f.file_field :team_img%><br>
  <%= f.label :チーム名%><br>
  <%= f.text_field :team_name%>
  <%= f.submit "更新"%>
  <% end %>
</div>
